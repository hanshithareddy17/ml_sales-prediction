# PostgreSQL (e.g. AWS RDS)
# You can use either POSTGRES_* or DB_* variables. The backend supports both.
POSTGRES_HOST=your-rds-endpoint.amazonaws.com
POSTGRES_DB=your_database_name
POSTGRES_USER=your_db_username
POSTGRES_PASSWORD=your_db_password
POSTGRES_PORT=5432

# Alternative naming (optional)
DB_HOST=your-rds-endpoint.amazonaws.com
DB_NAME=your_database_name
DB_USER=your_db_username
DB_PASSWORD=your_db_password
DB_PORT=5432

# Forecast source (recommended: db)
FORECAST_MODE=db

# If using live/precomputed mode, this can persist results (off by default)
SAVE_FORECAST_TO_DB=0

# Flask
FLASK_DEBUG=0

# Frontend (React) API base URL, used at build time
REACT_APP_API_URL=http://your-backend-hostname-or-elb:5000
